# Contributing

Use Zig 0.16.0, Node.js 24, and Bun 1.3.14. Install dependencies with `bun install` and run `bun run validate` before submitting a change. Behavior changes should include tests, and public interface changes should update the README.
